/**
 * DEPRECATED — RC9 Audit 2026-08-06
 *
 * This Zustand store is dead code. It was intended to replace EntityStore
 * with Zustand + TanStack Query but was never connected.
 *
 * Confirmed: zero imports anywhere in src/ (grep verified).
 * EntityStore (entityStore.ts) is the active store.
 *
 * DELETE THIS FILE after confirming no external references.
 * See .audit/rc9/modules.csv and STATE_OWNERSHIP.md
 */
