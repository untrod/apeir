"""Runtime service facades for public routes and clients.

Service modules are the boundary consumed by API, CLI, Shell, and SDK code.
They may bridge to compatibility adapters while legacy implementations are
being migrated into runtime-native stores.
"""

