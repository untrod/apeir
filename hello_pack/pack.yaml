name: hello_pack
version: 0.1.0
description: A Nous Runtime pack

capabilities:
  - hello_pack.hello

providers:
  - HelloProvider

dependencies:
  runtime: ">=1.0"

config:
  greeting: "Hello from hello_pack!"
