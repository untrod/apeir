# Migration Guide

During RC2 evaluation, migrate legacy session state through ConversationStore adapters, legacy execution through Run and Event contracts, provider secrets to credential references, and connector writes through Governance and Approval. Back up local state before migration and verify focused subsystem tests.
